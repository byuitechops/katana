export default function getStyles(elName) {
  return `
      .category-container {
        max-width: 100%
        box-sizing: border-box;
        display: flex;
        flex-wrap: wrap;
      }
    `;
}